Platystarter
===

A started theme based on _s theme

Getting Started
---------------
- Run `bower install`
- Run `sudo npm install`
- Run `gulp watch`

for *Production*
add `--type production` flag on `gulp` command
